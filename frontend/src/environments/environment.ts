// Configurare pentru build de productie
export const environment = {
  production: true,
  // In productie, API-ul poate fi pe alt domeniu / port.
  // Pentru demo, lasam acelasi URL ca in dezvoltare.
  apiUrl: 'http://localhost:8055'
};
